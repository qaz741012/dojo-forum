<%= render partial: "shared/user_profile", locals: { user: @user } %>

<%= render partial: "shared/post_draft_template", locals: { posts: @posts } %>
<div class="px-5 mt-4">
  <%= link_to "New Post", new_post_path, class: "btn btn-secondary" %>
</div>
